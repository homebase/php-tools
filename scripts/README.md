Do NOT run scripts from this directory directly.
Run the top-level `php-tools` command from the parent directory instead.
The `scripts/*.install` and `scripts/*.update` files are invoked by `php-tools` with `bash`.

Do NOT run scripts from this directory directly.
Run the top-level commands from the parent directory (`php-tools`) instead.
The `scripts/*.install` and `scripts/*.update` files are invoked by the top-level scripts with `bash`.
